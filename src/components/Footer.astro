---
// Footer component - used on all pages
---

<footer class="footer">
  <div class="footer-container">
    <div class="footer-logo">
      <img src="/images/logos/azure_logo_grey.svg" alt="Azure Ventures logo" loading="lazy">
    </div>
    
    <div class="footer-content">
      <nav class="footer-nav">
        <a href="/">VENTURES</a>
        <a href="/what-we-do">WHAT WE DO</a>
        <a href="/about">ABOUT US</a>
      </nav>
      
      <div class="footer-center">
        <h4>CONTACT</h4>
        <a href="mailto:contact@azure-vc.co">contact@azure-vc.co</a>
        <a href="mailto:hello@yachtingventures.co">hello@yachtingventures.co</a>
      </div>
      
      <div class="footer-right">
        <h4>CONNECT</h4>
        <a href="https://www.linkedin.com/company/azure-vc/posts/?feedView=all" class="linkedin-link" target="_blank" rel="noopener noreferrer" aria-label="Connect on LinkedIn">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="currentColor">
            <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
          </svg>
        </a>
      </div>
    </div>
    
    <div class="footer-bottom">
      <p>Copyright @ Azure Ventures. All rights reserved. Designed by <a href="https://kitectn.com/" target="_blank" rel="noopener noreferrer">Kite Brand Agency</a></p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-gray-medium);
    padding: var(--space-2xl) 0 var(--space-xl);
    position: relative;
    z-index: 2;
    margin-top: auto;
    
  }

  .footer-container {
    width: 100%;
    margin: 0 auto;
    padding: 0 clamp(1rem, 4vw, 3rem);
  }

  /* Logo in its own row */
  .footer-logo {
    margin-bottom: var(--space-lg);
  }

  .footer-logo img {
    height: clamp(32px, 4vw, 44px);
    width: 150px;
    display: block;
  }

  /* 3 columns below logo - 60/20/20 split */
  .footer-content {
    display: grid;
    grid-template-columns: 3fr 1fr 1fr;
    gap: clamp(2rem, 4vw, 4rem);
    align-items: start;
    padding-bottom: var(--space-2xl);
    border-bottom: 1px solid rgba(255, 255, 255, 0.15);
  }

  /* Column 1 - Navigation links (60%) */
  .footer-nav {
    display: block;
  }

  .footer-nav a {
    color: var(--color-gray-lighter);
    text-decoration: none;
    font-size: clamp(0.75rem, 1.5vw, 1rem);
    font-weight: 500;
    letter-spacing: 0.05em;
    transition: color 0.3s ease;
    display: block;
    margin-bottom: clamp(0.75rem, 1.5vw, 1rem);
  }

  .footer-nav a:hover {
    color: var(--color-white);
  }

  /* Column 2 - Contact (20%) */
  .footer-center {
    display: block;
  }

  .footer-center h4 {
    color: var(--color-gray-lighter);
    font-size: clamp(0.5rem, 1.5vw, 1rem);
    font-weight: 500;
    letter-spacing: 0.05em;
    margin: 0 0 0.75rem 0;
    padding: 0 0 0.75rem 0;
    border-bottom: 2px solid var(--color-gray-lighter);
    width: 100%;
  }

  .footer-center a {
    color: var(--color-gray-lighter);
    text-decoration: none;
    font-size: clamp(0.85rem, 1.8vw, 0.9rem);
    font-weight: 400;
    transition: color 0.3s ease;
    display: block;
    margin-bottom: clamp(0.5rem, 1vw, 0.75rem);
  }

  .footer-center a:hover {
    color: var(--color-white);
  }

  /* Column 3 - Connect (20%) */
  .footer-right {
    display: block;
  }

  .footer-right h4 {
    color: var(--color-gray-lighter);
    font-size: clamp(0.75rem, 1.5vw, 1rem);
    font-weight: 500;
    letter-spacing: 0.05em;
    margin: 0 0 0.75rem 0;
    padding: 0 0 0.75rem 0;
    border-bottom: 2px solid var(--color-gray-lighter);
    width: 100%;
  }

  .linkedin-link {
    color: var(--color-gray-lighter);
    transition: color 0.3s ease;
    display: inline-flex;
  }

  .linkedin-link:hover {
    color: var(--color-white);
  }

  .linkedin-link svg {
    width: clamp(24px, 3vw, 32px);
    height: clamp(24px, 3vw, 32px);
  }

  /* Bottom section */
  .footer-bottom {
    padding-top: var(--space-lg);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0;
    font-size: clamp(0.5rem, 1.4vw, 0.7rem);
  }

  .footer-bottom p {
    color: var(--color-gray-lighter);
    margin: 0;
    text-align: center;
     font-size: clamp(0.5rem, 1.4vw, 0.7rem);
  }

  .footer-bottom p:first-child::after {
    content: '.';
    margin: 0 0.25rem;
  }

  /* Mobile styles */
  @media (max-width: 768px) {
    .footer {
      padding: var(--space-2xl) 0 var(--space-lg);
    }

    .footer-logo {
      margin-bottom: var(--space-md);
    }

    .footer-content {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .footer-nav a {
      margin-bottom: 0.75rem;
    }

    .footer-center h4,
    .footer-right h4 {
      margin-bottom: 0.75rem;
      padding-bottom: 0.75rem;
    }

    .footer-center a {
      margin-bottom: 0.5rem;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 0.25rem;
      padding-top: var(--space-md);
    }

    .footer-bottom p:first-child::after {
      content: none;
    }
  }
</style>
